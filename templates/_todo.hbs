<li>
    <div class="one columns">
        <input type="checkbox"
            {{#if checked}}
                checked="checked"
            {{/if}}>
    </div>
    <div class="fourteen columns">
        <p>{{ title  }}</p>
    </div>
</li>
